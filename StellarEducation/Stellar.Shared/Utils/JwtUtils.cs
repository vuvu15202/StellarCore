/*
namespace Stellar.Shared.Utils;

using Microsoft.Extensions.Options;
using Microsoft.IdentityModel.Tokens;
using System.IdentityModel.Tokens.Jwt;
using System.Security.Claims;
using System.Text;
// using CourseService.Domain.Entities; // Circular dependency
using Stellar.Shared.Helpers;

public interface IJwtUtils
{
    // public string GenerateJwtToken(User user);
    public int? ValidateJwtToken(string token);
}

public class JwtUtils : IJwtUtils
{
    // Implementation commented out to allow build of Shared library without Domain dependency.
    // Refactor to use generic ClaimsPrincipal or remove if unused.
}
*/
